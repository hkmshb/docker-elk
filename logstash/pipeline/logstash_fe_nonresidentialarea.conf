input {
  jdbc {
    jdbc_driver_library => "/usr/share/java/postgresql-9.4-1201.jdbc41.jar"
    jdbc_driver_class => "org.postgresql.Driver"
    jdbc_connection_string => "${JDBC_STRING}"
    jdbc_user => "${PSQL_USER}"
    jdbc_password => "${PSQL_PASSWORD}"
    schedule => "*/3 * * * *"
    statement_filepath => "/usr/share/logstash/sql/fe_nonresidentialarea.sql"
    clean_run => false
    tracking_column => "timestamp"
    tracking_column_type => "timestamp"
    record_last_run => true
    use_column_value => true
    jdbc_validate_connection => true
    type => "nonresidential-area"
  }
}

filter{
  if [type] == "nonresidential-area" {
    date {
      match => [ "timestamp" , "yyyy-MM-dd HH:mm:ssZ", "ISO8601" ]
      timezone => "Africa/Lagos"
    }
  }
}

filter{
  if [type] == "nonresidential-area" {
    if [timestamp] == [null] {
      mutate {
        replace => {"timestamp" => "2016-12-30T16:30:02.660Z"}
      }
    }
  }
}

output {
  if [type] == "nonresidential-area" {
    stdout { codec => rubydebug }
    elasticsearch {
      index => "settlement-nonresidentialarea"
      document_type => "settlement-nonresidentialarea"
      manage_template => true
      template => "/usr/share/logstash/mapping/fe_nonresidentialarea.json"
      template_name => "settlement-nonresidentialarea"
      template_overwrite => true
      document_id => "%{globalid}"
      hosts => ["elasticsearch:9200"]
    }
  }
}